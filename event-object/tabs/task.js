const tabs = Array.from(document.querySelectorAll(".tab"));
const tabsContent = Array.from(document.querySelectorAll(".tab__content"));
function turnOn() {
    this.indexOf("")
}


tabs.forEach(tab => tab.addEventListener("click", turnOn))
